{
  "name": "morse-ws",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "bun run --filter=\"client\" build && pm2 start pm2.config.js"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "prettier": "^3.4.2",
    "prettier-plugin-svelte": "^3.3.2",
    "prettier-plugin-tailwindcss": "^0.6.9"
  }
}
